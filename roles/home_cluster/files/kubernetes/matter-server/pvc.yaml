apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matter-server
spec:
  accessModes:
    - ReadWriteOnce
  selector:
    matchLabels:
      app.kubernetes.io/name: matter-server
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client
