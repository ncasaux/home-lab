apiVersion: v1
kind: Service
metadata:
  name: openthread-border-router
spec:
  type: ClusterIP
  ports:
    - name: web
      port: 80
    - name: rest
      port: 8080
  selector:
    app: openthread-border-router
